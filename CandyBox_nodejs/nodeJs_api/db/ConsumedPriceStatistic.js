class ConsumedPriceStatistic {
    constructor(price, consumed) {
        this.price = price;
        this.consumed = consumed
    }

    price;
    consumed
}

// eslint-disable-next-line no-undef
module.exports = {
    ConsumedPriceStatistic: ConsumedPriceStatistic,
};
